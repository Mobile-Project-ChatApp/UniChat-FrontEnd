export default interface User {
  // id: number;
  username: string;
  email: string;
  passwordHash: string;
  profilePicture: string;
  createdAt: string;
  // IsAdmin: Boolean;
  // status: string;
}
